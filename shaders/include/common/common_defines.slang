#ifndef COMMON_DEFINES_H
#define COMMON_DEFINES_H

#define SAMPLE_TEX(TEXTURE, SAMPLER, TEXCOORDS) TEXTURE.Sample(SAMPLER, TEXCOORDS)
#define SAMPLE_TEX_LEVEL(TEXTURE, SAMPLER, TEXCOORDS, LEVEL) TEXTURE.SampleLevel(SAMPLER, TEXCOORDS, LEVEL)

#define SAMPLE_ARR_TEX(ARRAY, IDX, SAMPLER, TEXCOORDS) SAMPLE_TEX(ARRAY[IDX], SAMPLER, TEXCOORDS)
#define SAMPLE_ARR_TEX_LEVEL(ARRAY, IDX, SAMPLER, TEXCOORDS, LEVEL) SAMPLE_TEX_LEVEL(ARRAY[IDX], SAMPLER, TEXCOORDS, LEVEL)

#endif